var originalValue = 3000;

// Percentage to subtract
var percentage = 20;

// Calculate the result
var result = originalValue - (originalValue * percentage / 100);

// Log the result
console.log(result);